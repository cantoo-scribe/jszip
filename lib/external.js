"use strict";

/**
 * Let the user use/change some implementations.
 * The global Promise is used by default (the environment may provide a
 * custom implementation, see zone.js).
 */
module.exports = {
    Promise: Promise
};
